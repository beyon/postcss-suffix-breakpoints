// Might come in handy, considered using with
// root.walk(classSelectorRegex, ruleNode => {} )
// but wasn't a very good idea in that place
const classSelectorRegex =
    /^\.-?[_a-zA-Z]+[_a-zA-Z0-9-]*(:[_a-zA-Z0-9-]+)*/;
